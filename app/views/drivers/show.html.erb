 <h3 class='text-center'>All Reservations Assigned to: <%= @driver.name%></h3>
<div class="row">
  <div class="offset-4 col-md-4">
      <p class='text-center'>
        <% @driver.reservations.each do |r| %>
        <ul>
    <li>Date: <%= readable_date(r) %></li>
    <li>Time: <%= r.time %></li>
    <li>Vehicle Type: <%= r.vehicle_type %></li>
    <li>Pick Up: <%= r.pick_up %></li>
    <li>Drop Off: <%= r.drop_off %></li>
       </ul><br>
        <% end %>
      </p>
  </div>
</div>

